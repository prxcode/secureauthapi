server:
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/authdb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379