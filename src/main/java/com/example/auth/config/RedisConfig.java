package com.example.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {

    // redis configuration can be extended here
    // spring boot auto config handles basic setup
}